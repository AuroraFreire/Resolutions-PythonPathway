[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week1_AuroraFreire"
version = "0.0.1"
dependencies = []

[project.scripts]
todo = "resolution_week1_AuroraFreire.main:main"